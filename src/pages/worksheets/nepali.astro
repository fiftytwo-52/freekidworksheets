---
// /worksheets/nepali — page 1 of the Nepali-only worksheet library (§11.2).
// Opened from the Nepali version of the site (/nepali); shows Nepali
// worksheets only. Pages 2+ are emitted by ./page/[page].astro.
import LibraryPage from '../../components/LibraryPage.astro';
---

<LibraryPage page={1} lang="ne" />
